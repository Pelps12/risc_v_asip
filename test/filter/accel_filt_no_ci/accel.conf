# compute_filt synthesized in hardware, but no custom instruction opcode.
# Program runs software filter loop on a processor that physically contains
# the filter hardware — meaningful area/cycle comparison vs accel_filt.
ACCEL_FILT_HW
